#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if not matrix:
        return None
    else:
    for s in matrix
        if len(s) == 0:
            print()
    for i range(len(s)):
        print("{:d}".format(s[i])),
            end="\n" if i is len(s) - 1 else " ")
